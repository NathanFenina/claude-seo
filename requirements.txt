# Claude Code SEO Décupler — dépendances Python
#
# Installation :  pip install -r requirements.txt
#
# Les scripts guard.py, doctor.py, internal_pagerank.py, gsc_analyse.py et
# cms_restore.py (--lister) fonctionnent SANS aucune de ces dépendances.
# Elles ne sont nécessaires que pour ce qui lit le web.

beautifulsoup4>=4.12.0,<5.0.0     # parsing HTML
requests>=2.32.4,<3.0.0           # requêtes HTTP
lxml>=6.0.2,<7.0.0                # parseur rapide, optionnel mais recommandé
urllib3>=2.6.3,<3.0.0             # correctifs de sécurité

# Optionnel — captures d'écran et analyse visuelle.
# Le MCP Chrome DevTools couvre les mêmes besoins sans cette dépendance.
playwright>=1.56.0,<2.0.0
Pillow>=12.1.0,<13.0.0
